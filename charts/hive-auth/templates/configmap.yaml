apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hive-auth.configMapName" . }}
  labels:
    {{- include "hive-auth.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
