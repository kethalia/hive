apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hive-terminal.configMapName" . }}
  labels:
    {{- include "hive-terminal.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
