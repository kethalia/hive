# Hive Worker Workspace

Use the assigned repository, task prompt, Git history, and `AGENTS.md` files as the source of truth. Keep changes scoped, preserve user work, and run the narrowest relevant checks before broader validation.

Codex and Claude Code use the configured Playwright MCP server. Do not require or sync an Obsidian vault.
