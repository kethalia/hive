# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M001 | convention | Git push policy | Never push directly to main — all changes via PR | Branch protection enforced. Both human and agent commits go through PRs with squash merge. Consistent with the Hive philosophy of verification before merge | No | agent |
| D002 | M001/S01 | architecture | Job queue technology for task dispatch | BullMQ with IORedis (maxRetriesPerRequest: null) | Battle-tested Redis-backed queue with TypeScript support, built-in concurrency control (enables R008 parallel execution), retries, and progress tracking. IORedis must use maxRetriesPerRequest:null or BullMQ workers fail with timeout errors. | Yes | agent |
| D003 |  | architecture | How to handle pre-existing ioredis/bullmq type conflicts blocking next build | Set typescript.ignoreBuildErrors: true in next.config.ts | The project has duplicate ioredis versions (top-level ioredis vs bullmq's bundled ioredis) causing type incompatibilities that prevent next build from succeeding. Runtime behavior is correct. Fixing requires aligning ioredis versions across deps, which is outside dashboard scope. The ignoreBuildErrors flag lets the build proceed while keeping tsc available for targeted type-checking. | Yes — fix when ioredis/bullmq versions are aligned | agent |
| D004 | M001/S01 | M001/S01 | ORM choice for Hive orchestrator | Prisma instead of Drizzle ORM | User preference. Prisma provides a more familiar API with auto-generated client, built-in migrations, and better ecosystem support. Schema-first approach with prisma/schema.prisma is the single source of truth for DB models. | No | agent |
| D005 | M001/S01,S02 | M001/S01,S02 | Server action pattern for dashboard mutations and data fetching | next-safe-action v8 + zod for type-safe validated server actions; remove API routes | Server actions are the canonical Next.js App Router pattern for mutations. next-safe-action adds input/output validation via zod schemas, typed error handling, and useAction/useOptimisticAction hooks. Eliminates API route boilerplate since all consumers are the dashboard UI (no external callers). If worker workspaces need to call back, we'll add a targeted API route then. | Yes | agent |
| D006 | M001/S03 | architecture | Blueprint execution approach for worker agent | Sequential async functions via coder ssh, not a generic engine or DSL | Per R025 (Stripe's blueprint pattern): blueprints are TypeScript functions, not YAML/visual. Each step is an async function receiving a shared BlueprintContext. Runner calls them in order. Remote execution uses child_process.execFile("coder", ["ssh", workspace, "--", "bash", "-l", "-c", command]) — simpler than SSH libraries (no key management), leverages existing Coder auth. Pi runs in --print mode (non-interactive, exits after completion) with assembled context piped via temp file to avoid shell arg length limits. | No | agent |
| D007 | M001/S05 | architecture | Verifier failure handling policy | Verifier failure is informational — task still set to done with inconclusive report | The PR exists regardless of whether verification succeeds. Blocking task completion on verifier failure would prevent valid PRs from being surfaced. The verification report's outcome field (pass/fail/inconclusive) communicates verification status without blocking the pipeline. | Yes | agent |
| D008 | M001/S05 | architecture | Workspace lifecycle pattern for tasks with verification | Dual-workspace lifecycle — worker + verifier tracked independently, both cleaned up in finally block | A task may spawn two workspaces (worker + verifier). Both must be tracked and cleaned up regardless of success/failure. The finally block pattern ensures cleanup happens even on errors. Independent tracking allows different template types and variable sets. | Yes | agent |
| D009 | M002/S01 | architecture | Fan-out pattern for council parallel reviewer jobs | BullMQ FlowProducer with council-reviewer (child jobs) and council-aggregator (parent job) queues | FlowProducer gives atomic fan-out + guaranteed fan-in with no polling. Parent enters waiting-children state until all N reviewers complete. continueParentOnFailure:true enables partial aggregation when some reviewers fail — consistent with informational failure policy (D007). | Yes — upgrade to DAG-based flow if council needs multi-stage review rounds | collaborative |
| D010 | M002 | architecture | Agent harness for council reviewer workspaces | claude --print (Anthropic's official Claude Code CLI) instead of Pi/GSD | User explicitly requested migration away from Pi towards Claude CLI for all new agent work. Council is the first slice to use Claude CLI. Invocation pattern is identical: --print for non-interactive mode, context piped via base64-encoded temp file (D006 pattern). | No — deliberate move to Claude CLI for all new work | human |
| D011 | M002/S01 | architecture | Coder template strategy for council reviewer workspaces | New hive-council Terraform template (templates/hive-council/) — separate from hive-worker and hive-verifier | Council's blueprint is read-only review; fundamentally different from worker (implement) and verifier (run/test). Independent template evolves without risk of breaking other templates. Based on hive-worker structure but with Claude CLI instead of Pi, no post-agent tools. | Yes — could merge with verifier template if they converge | collaborative |
| D012 | M002/S02 | architecture | Council reviewer finding schema | { findings: [{ file, startLine, severity: critical\ | major\ | minor\ | agent |
| D013 | M002/S03 | architecture | Council findings aggregation algorithm | Group by file + startLine; count >= 2 = consensusItem | Deterministic, no extra API calls, maps to how human reviewers think about code review ("both flagged line 42 in auth.ts"). Simple to unit-test exhaustively. Semantic similarity deferred — overkill for MVP. | Yes — add semantic similarity grouping if file+line misses too many true consensus items | collaborative |
| D014 | M002/S01 | architecture | Pipeline position of verifier and council steps | Verifier promoted from fire-and-forget to awaitable step 9; council is step 10 | Council reviews after verifier proves the code works — ensures we're not reviewing code that fails verification. Sequential ordering chosen over parallel (council + verifier simultaneously) for simpler orchestration and clearer semantics. | Yes — run council parallel with verifier if pipeline latency becomes a concern | collaborative |
| D015 | M002/S01 | architecture | Council failure handling policy | Council failure is informational — task stays done; councilReport.outcome communicates quality (complete/partial/inconclusive) | Mirrors verifier failure policy (D007). The PR exists regardless of whether council review succeeds. Blocking task completion on council failure would hide valid PRs. postedCommentUrl=null on comment failure; councilReport still persisted. | Yes — if council is made a blocking gate in the future | collaborative |
| D016 |  | architecture | Type narrowing for JSON fields on TaskWithRelations | Use unknown + isCouncilReport() type guard, narrowing happens at render time | Avoids premature serialization/deserialization of Prisma JsonValue. Guard pattern is clear, testable, and compatible with component prop types. | Yes | agent |
| D017 |  | library | Form field validation coercion for numeric inputs | Use z.coerce.number() in Zod schema for councilSize, export schema for unit testability | HTML form data arrives as strings. z.coerce.number() is declarative and handles conversion at the schema layer. Exporting schema allows direct unit tests without coupling to action layer. | Yes | agent |
| D018 | M005 | architecture | Terminal transport for interactive workspace shells | Proxy Coder's native PTY WebSocket (/api/v2/workspaceagents/{id}/pty) through Next.js API route | Coder handles PTY allocation, resize, UTF-8, and auth. Avoids managing SSH child processes and custom resize protocol. Alternative (spawning coder ssh per terminal) rejected for complexity. | Yes — if Coder removes the PTY WebSocket endpoint | collaborative |
| D019 | M005 | architecture | Terminal session persistence strategy | All terminals always run inside tmux sessions — no bare shells. Browser disconnect = tmux detach, reconnect = tmux attach with scrollback. | Persistence guarantee by design. tmux is already baked into all workspace templates. No bare shell option simplifies reconnection model. | No | collaborative |
| D020 | M005 | architecture | External tool integration approach for workspace detail page | Iframe-embed Filebrowser and KasmVNC with popup-out button; link-out for Coder dashboard. Automatic fallback to popup if iframe blocked. | Rich integrated experience for workspace-scoped tools. Coder dashboard is a full app better suited to its own tab. Iframe fallback ensures feature always works regardless of X-Frame-Options. | Yes — if iframe embedding proves unreliable across Coder versions | collaborative |
| D021 | M005 | convention | Workspace lifecycle ownership boundary | Workspace creation/deletion permanently out of scope for Hive — handled by Coder, linked from dashboard (R041) | Avoids reimplementing Coder's workspace lifecycle. Coder already does this well. Hive focuses on what Coder doesn't provide: persistent interactive terminals in the browser. | No — permanent exclusion | human |
| D022 | M006 | architecture | Scrollback storage backend for persistent terminal sessions | Postgres with chunked batch writes (not Redis) | User needs scrollback to persist for days. Postgres is already the durable store and survives restarts. Redis is in-memory and loses data on restart. Write latency acceptable with 5s/1000-line batching. | No | collaborative |
| D023 | M006 | architecture | WebSocket reconnection strategy for terminal sessions | Infinite retries with exponential backoff capped at 60s (replaces 10-attempt hard limit) | Current 10-attempt limit causes permanent session death after ~2 minutes. User requirement: nothing closes automatically, ever. No finite retry count is acceptable. | No | collaborative |
| D024 | M006 | architecture | Workspace keep-alive mechanism for terminal persistence | Server-side keep-alive pings to Coder API, independent of browser state | Browser tabs get suspended by OS. Browser-side pings die when browser closes. Only server-side pings guarantee workspace stays alive. Tracks workspaces with active terminal sessions and pings Coder's activity API on schedule. | No | collaborative |
| D025 | M006 | architecture | Virtual scrolling approach for unlimited terminal history | Custom virtual scroll layer over xterm.js with Postgres-backed chunk loading | User explicitly requested virtual lists for browser memory management. Sessions running for days accumulate 100K+ lines. Load visible viewport + buffer into xterm.js, lazy-load older chunks on scroll-up. | Yes — if xterm.js addon approach proves simpler during S04 planning | collaborative |
| D026 |  | architecture | Paginated scrollback API response format for virtual scroll consumption | Paginated path returns JSON with per-chunk objects (seqNum + base64 data + totalChunks) instead of concatenated binary. Non-paginated hydration path remains binary for backward compat. | Virtual scroll needs individual chunk boundaries and seqNums for cursor tracking and per-row rendering. Concatenated binary loses chunk boundaries. The hydration path (T02) only needs the most recent data written to xterm in one shot, so binary is fine there. Two response formats from one endpoint, selected by presence of cursor/limit params. | Yes — could unify to JSON-only if the binary hydration path proves unnecessary | agent |
| D027 | M007 | architecture | Sidebar layout approach for directory-tree navigation | shadcn SidebarMenuSub/SidebarMenuSubItem/Collapsible primitives for nested tree structure | Already installed, accessible, styled. Two levels of nesting don't warrant a custom recursive tree component. | No | collaborative |
| D028 | M007 | architecture | Sidebar display mode strategy | SidebarProvider collapsible prop toggles between offcanvas (floating) and sidebar (docked). Default offcanvas. Preference persisted in localStorage. Pin/unpin toggle at sidebar bottom. | User wants full terminal width by default but option to pin. shadcn handles both modes natively. ResizeObserver already handles terminal refit on width changes. | No | collaborative |
| D029 | M007 | architecture | Header and breadcrumbs removal scope | Remove header bar (HeaderContent) from all pages globally. Replace with floating SidebarTrigger button positioned absolute in top-left corner. | User explicitly wants zero chrome on all pages, not just terminals. Floating trigger keeps sidebar accessible without consuming layout space. | No | human |
| D030 | M007 | architecture | Terminal keystroke capture strategy | Auto-focus xterm on page mount and on any click within terminal content area. Sidebar interactions don't fight for focus. | Terminal-first UX. Sidebar clicks naturally shift focus; returning to terminal area recaptures. Global keyboard forwarding rejected as fragile and would break browser shortcuts. | Yes — if edge cases emerge with specific keyboard shortcuts | collaborative |
| D031 | M007 | architecture | Workspace tool buttons in sidebar | Filebrowser, KasmVNC, Code Server open in new browser tabs (external links). Terminal is a collapsible section with sessions underneath. | First three tools are full applications better suited to their own tabs. Terminal sessions are the primary in-app navigation target. Supersedes iframe-embed approach from M005/S04 for sidebar context. | Yes — if users want inline tool panels back | human |
| D032 | M008 | convention | Changeset versioning strategy for Hive monorepo | Independent versioning (not fixed) — each package versions independently | hive-orchestrator and hive-terminal-proxy are separate services with separate Dockerfiles and independent deployment. A change to terminal-proxy shouldn't bump the main app version. Differs from lsp-indexer which uses fixed versioning for its published SDK packages. | No | human |
| D033 | M008 | convention | Docker image registry and namespace for Hive | ghcr.io/kethalia/hive and ghcr.io/kethalia/hive-terminal-proxy | Hive belongs to kethalia org (not chillwhales). Consistent with existing ghcr.io/kethalia/hive-base namespace. | No | human |
| D034 | M008 | convention | npm publish for Hive packages | No npm publish — changesets used only for version tracking and Docker image tagging | Both packages are private: true with no npm consumers. The deliverable is Docker images, not npm packages. | Yes — if packages need to be consumed externally | human |
| D035 | M008 | convention | Compose file naming convention | docker-compose.yml (prod, published images), docker-compose.local.yml (build from source), docker-compose.dev.yml (postgres+redis only, unchanged) | User specified: prod compose is the default, local builds from source via explicit -f flag, dev stays as-is. Default docker compose up should use published images for deployment. | No | human |
| D036 | M009 | architecture | Vault sync target directories | Three independent copy targets: ~/.claude/, ~/.agents/, ~/.pi/agent/ — no symlinks | Claude Code reads ~/.claude/, Pi reads ~/.pi/agent/, ~/.agents/ is the cross-tool convention. Symlinks break across container mounts and tool path resolution. Direct copies are more robust. | Yes — if a tool adds a new discovery directory | human |
| D037 | M009 | architecture | Remove GSD agent skills symlink | Delete link_gsd_skills() entirely — GSD discovers skills from ~/.claude/ or ~/.agents/ | GSD's init already installs to ~/.agents/skills/ with symlinks from ~/.claude/skills/ and ~/.pi/skills/. The dedicated ~/.gsd/agent/skills symlink was a workaround no longer needed with multi-target direct copies. | No | human |
| D038 | M010 | architecture | Authentication flow for multi-user Coder access | Coder's direct login API (POST /api/v2/users/login) instead of OAuth2 PKCE | OAuth2 PKCE requires the oauth2 experiment flag on the Coder server, which many deployments won't have enabled. Direct login is available on all Coder instances without extra configuration. Hive handles raw credentials only in transit (HTTPS) and never stores passwords. | Yes — if Coder makes OAuth2 generally available without experiment flag | collaborative |
| D039 | M010 | architecture | Session and token storage for multi-user auth | Database-backed sessions in Postgres (User, CoderToken, Session tables) with encrypted session cookie | Workers need persistent token access for background jobs hours after submission. A user model enables task ownership tracking. Encrypted cookie-only approach rejected because cookies have size limits and can't store rotation metadata. | No | collaborative |
| D040 | M010 | architecture | Worker credential strategy for background jobs | Long-lived Coder API keys (30-day default) created after login, stored encrypted, rotated at 75% lifetime via BullMQ job | Session tokens expire in 24h by default — too short for workers processing tasks hours later. API keys have configurable lifetime. Static service token rejected per user preference for per-user credential isolation. API keys cannot be refreshed, so rotation creates new key and deletes old transactionally. | No | collaborative |
| D041 | M010 | architecture | Token encryption at rest | AES-256-GCM via Node.js crypto, key from TOKEN_ENCRYPTION_KEY env var | GCM provides authenticated encryption (integrity + confidentiality). Single env var is the only required secret. Per-user graceful degradation on key change (detect GCM auth tag mismatch, mark token invalid, redirect to login rather than crash). | No | collaborative |
| D042 | M010 | architecture | User identity uniqueness constraint | Unique per (coderUrl, coderUserId), not per email | Same email can exist on multiple Coder deployments. Same Coder deployment can have multiple users. Email alone is not a stable unique identifier across deployments. | No | collaborative |
| D043 | M010 | architecture | Logout behavior scope | Logout deletes browser session only — API key and user record persist | In-flight worker jobs must not be disrupted by logout. API key stays for background work and gets cleaned up by rotation job. Re-login creates new browser session without needing new API key. | Yes — if explicit "revoke all sessions" feature added | human |
